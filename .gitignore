.venv/
__pycache__/
*.pyc
.pytest_cache/
.env
.idea/
# Local tool permissions — machine-specific, not project configuration.
.claude/settings.local.json

# Uploaded and generated images. The demo photo is regenerated at startup.
media/

# Runtime SQLite files (analytics.db, app.db, e2e-*.db). Anchored to the repo
# root with a leading slash so it does NOT also ignore app/data/ — that holds
# catalog.yaml, a tracked source file.
/data/
